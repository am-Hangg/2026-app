# React
## virtual DOM
- wherever state change 
## JSX
- JS + HTML code

# Components
- UI Block
- Reusable
- functional component, class components
- functional components are stateless components by default
- functional components are stateful components
- stateful: state/UI can be update
- stateless: state can update, UI can not update.
- Hooks: only special function

# Props

- Properties of Components
- Similar to HTML element attribute
